const JOIN_SUCCESS = 'join_Room'
const REQUEST = 'request'
const JOIN_FAILURE = 'join_failure'
const ROOM_ID = 'room_id'
const AFTER_POST_MESSAGE = 'post_message'
const GET_CONVERSATIONS = 'get_user_conversations'
module.exports = {
    JOIN_SUCCESS,
    REQUEST,
    JOIN_FAILURE,
    ROOM_ID,
    AFTER_POST_MESSAGE,
    GET_CONVERSATIONS

}